# MODE validate

void main() {
  # ERROR message="Invalid left-hand-side expression; cannot be assigned to"
  12 = 10

  # ERROR message="Invalid left-hand-side expression; cannot be assigned to"
  false = true
}