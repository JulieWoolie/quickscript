# MODE run
# DISABLE-STAT-INLINING = true
# DISABLE-EXPR-INLINING = true

void main() {
  uint32[] arr = [3, 4, 5]
  assert arr.length == 3 : "Array is not of length 3"
  assert arr[0] == 3 : "arr[0] != 3"
  assert arr[1] == 4 : "arr[1] != 4"
  assert arr[2] == 5 : "arr[2] != 5"
}