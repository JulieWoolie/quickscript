
uint32[] f(uint32 a, uint32 b) {
  return [a, b]
}

void main() {
  uint32[] first = f(1, 2)
  assert first.length == 2: "first.length == 2: first: " + first
  assert first[0] == 1 : "first[0] == 1, first: " + first
  assert first[1] == 2 : "first[1] == 2, first: " + first
}